services:
  it-tools:
    image: corentinth/it-tools:latest
    container_name: it-tools
    env_file:
      - .env
    ports:
      - ${LAN_IP}:8083:80
      - ${TAILSCALE_IP}:8083:80
    restart: unless-stopped
