services:
  myspeed:
    image: germannewsmaker/myspeed
    container_name: MySpeed
    ports:
      - ${LAN_IP}:5216:5216
      - ${TAILSCALE_IP}:5216:5216
    volumes:
      - myspeed:/myspeed/data
    restart: always

volumes:
  myspeed:
